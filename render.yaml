services:
  - type: web
    name: taskwave-api
    runtime: docker
    rootDir: taskwave_backend
    plan: free                  # ← 이것이 핵심!
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: taskwave-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: STORAGE_BACKEND
        value: local
      - key: MEDIA_ROOT
        value: /app/media

  - type: web
    name: taskwave-app
    runtime: static
    rootDir: taskwave-app
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: taskwave-db
    plan: free                  # ← DB도 반드시 free로!
